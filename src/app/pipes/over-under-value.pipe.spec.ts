import { OverUnderValuePipe } from './over-under-value.pipe';

describe('OverUnderValuePipe', () => {
  it('create an instance', () => {
    const pipe = new OverUnderValuePipe();
    expect(pipe).toBeTruthy();
  });
});
