import { SpreadValuePipe } from './spread-value.pipe';

describe('SpreadValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SpreadValuePipe();
    expect(pipe).toBeTruthy();
  });
});
