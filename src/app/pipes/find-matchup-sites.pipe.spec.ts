import { FindMatchupSitesPipe } from './find-matchup-sites.pipe';

describe('FindMatchupLinePipe', () => {
  it('create an instance', () => {
    const pipe = new FindMatchupSitesPipe();
    expect(pipe).toBeTruthy();
  });
});
