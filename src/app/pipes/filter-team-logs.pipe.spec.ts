import { FilterTeamLogsPipe } from './filter-team-logs.pipe';

describe('FilterTeamLogsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeamLogsPipe();
    expect(pipe).toBeTruthy();
  });
});
